sqlglot>=25,<31
# YAML parsing for the copied-in Semantic Catalog (app/semantic_catalog/, D83/D84).
PyYAML==6.0.3
fastapi==0.124.4
python-multipart==0.0.20
# XLSX parsing for the external-dataset upload front door (UI Slice 4). Read in
# read_only + data_only mode on byte-capped input (no formula eval, streamed rows).
openpyxl==3.1.5
# openpyxl auto-detects defusedxml and routes its XML parsing through it, guarding
# against XML entity-expansion (billion-laughs) on untrusted uploads. Explicit dep.
defusedxml==0.7.1
uvicorn[standard]==0.32.1
clickhouse-connect==0.7.16
pydantic-settings==2.7.1
pydantic==2.11.7
python-dotenv==1.0.1
# MCP SDK — requires Python >= 3.10.  The Docker image uses Python 3.11-slim.
# Dependency resolution: mcp 1.27.2 requires pydantic>=2.11, pydantic-settings>=2.5.2,
# uvicorn>=0.31.1, and sse-starlette 3.4.4 requires starlette>=0.49.1.
# fastapi 0.124.4 resolves starlette to 0.50.0 which satisfies both constraints.
mcp==1.27.2
starlette==0.50.0
# JWT/OIDC validation. [crypto] pulls in `cryptography` for RS*/ES*/PS* signature
# verification; PyJWKClient fetches the IdP JWKS over stdlib urllib (no extra dep).
PyJWT[crypto]==2.13.0
# Employee-access freshness cache (app/employee_access.py). Only imported when
# REDIS_URL is configured; a down/absent Redis degrades to the ClickHouse
# freshness read (correctness never depends on Redis).
redis==5.2.1
