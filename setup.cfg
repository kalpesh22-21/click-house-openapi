[tool:pytest]
testpaths = tests
markers =
    integration: marks tests as integration tests requiring a live stack (skipped by default)
    real_jwks: opt out of the autouse JWKS patch so the real JWKS-fetch path runs
